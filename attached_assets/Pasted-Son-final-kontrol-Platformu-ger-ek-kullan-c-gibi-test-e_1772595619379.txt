Son final kontrol. Platformu gerçek kullanıcı gibi test et. Yeni özellik EKLEME — sadece kalan hataları bul ve düzelt.

FİNAL TEST — GERÇEK KULLANICI SENARYOLARI:

1. SENARYO: YENİ KULLANICI KAYIT AKIŞI
   - /register sayfasını aç
   - 4 rol kartı görünüyor mu? (Ship Agent, Shipowner, Ship Broker, Ship Provider)
   - Admin seçeneği OLMAMALI — varsa KALDIR
   - Ship Agent kartını seç
   - Kayıt formunu doldur (test verisiyle)
   - Kayıt başarılı → dashboard'a yönleniyor mu?
   - Dashboard agent'a özel mi?
   - Top tabs agent sekmeleri mi gösteriyor?
   - Logout yap
   - Aynısını Shipowner, Ship Broker, Ship Provider için tekrarla (zihinsel kontrol yeterli, her rolün dashboard component'i ve menüsü doğru mu bak)

2. SENARYO: AGENT GÜNLÜK İŞ AKIŞI
   - Agent olarak login
   - Dashboard'da aktif seferler kartı var mı?
   - Voyages sayfasına git → liste yükleniyor mu?
   - "Yeni Sefer" butonu var mı? Tıkla → form açılıyor mu?
   - Formu kaydet → liste güncelleniyor mu?
   - Proformas sayfasına git → liste yükleniyor mu?
   - Quick Proforma sayfasına git → liman seçimi çalışıyor mu?
   - Tenders sayfasına git → liste yükleniyor mu?
   - Messages sayfasına git → yükleniyor mu?

3. SENARYO: OWNER FİLO KONTROLÜ
   - Owner olarak login
   - Dashboard'da filo haritası var mı? (veya en azından hata vermiyor mu?)
   - Vessels/Fleet sayfasına git → gemi listesi
   - Bunker Management → yükleniyor mu?
   - Fixtures sayfasına git → yükleniyor mu?
   - Compliance sayfasına git → yükleniyor mu?

4. SENARYO: BROKER KİRALAMA İŞ AKIŞI
   - Broker olarak login
   - Dashboard'da fixture kartları var mı?
   - Fixtures sayfasına git → liste
   - Cargo Positions → liste
   - Vessels/Fleet Tracking → yükleniyor mu?
   - Market Data → yükleniyor mu?

5. SENARYO: PROVIDER HİZMET AKIŞI
   - Provider olarak login
   - Dashboard'da gelen talepler var mı?
   - Service Requests → liste
   - Invoices → liste
   - Service Ports → yükleniyor mu?

6. ÇAPRAZ ROL ERİŞİM TESTİ:
   Her rol için URL'ye direkt yazarak erişim kontrolü yap:
   
   Agent olarak login olmuşken:
   - /bunker-management → erişim engeli veya yönlendirme olmalı
   - /fixtures → erişim engeli olmalı
   - /admin → erişim engeli olmalı
   
   Provider olarak login olmuşken:
   - /voyages → erişim engeli olmalı
   - /proformas → erişim engeli olmalı
   - /vessels → erişim engeli olmalı
   - /bunker-management → erişim engeli olmalı
   - /admin → erişim engeli olmalı
   
   Eğer erişim kontrolü YOKSA veya ÇALIŞMIYORSA:
   - client/src/lib/route-permissions.ts dosyası var mı? Yoksa oluştur
   - ProtectedRoute component'i var mı? Yoksa oluştur
   - App.tsx'te route'lar ProtectedRoute ile sarılmış mı? Sarılmamışsa sar
   - En azından yetkisiz sayfaya gidenler /dashboard'a yönlendirilsin

7. API GÜVENLİK TESTİ:
   Backend'de her endpoint'in rol kontrolü var mı?
   Şu endpoint'leri kontrol et:
   - GET /api/bunker → sadece shipowner, ship_broker, admin
   - GET /api/fixtures → sadece shipowner, ship_broker, admin
   - POST /api/proformas → sadece ship_agent, ship_broker, admin
   - GET /api/admin/* → sadece admin
   - POST /api/service-offers → sadece ship_provider, admin
   
   requireRole middleware KULLANILMAYAN endpoint varsa EKLE.
   En azından şu kritik endpoint'lerde olsun:
   - /api/admin/* → requireRole("admin")
   - /api/bunker/* → requireRole("shipowner", "ship_broker")
   - /api/fixtures/* → requireRole("shipowner", "ship_broker")
   - /api/service-offers/* → requireRole("ship_provider")
   - /api/service-ports/* → requireRole("ship_provider")

8. VERİ BÜTÜNLÜĞÜ:
   - Vessel oluşturulunca vessels tablosuna kaydediliyor mu?
   - Voyage oluşturulunca voyages tablosuna kaydediliyor mu?
   - Proforma oluşturulunca proformas tablosuna kaydediliyor mu?
   - Silinen kayıt gerçekten siliniyor mu?
   - Güncellenen kayıt gerçekten günceleniyor mu?
   - Foreign key olan yerlerde ilişkili kayıt yoksa hata vermiyor mu?

9. PERFORMANS:
   - Sayfa yüklenme süreleri makul mü? (3 saniyeden fazla süren var mı?)
   - Gereksiz re-render var mı?
   - useQuery'lerde staleTime ayarlanmış mı? (gereksiz tekrar fetch önleme)
   - Liste sayfalarında pagination çalışıyor mu? (tüm veriyi çekmiyor mu?)

10. SON GÖRSEL KONTROL:
    - Kırık layout var mı? (elemanlar üst üste biniyor mu?)
    - Boşta kalan alan çok fazla mı?
    - Text overflow var mı? (uzun gemi isimleri taşıyor mu?)
    - Button'lar tıklanabilir görünüyor mu?
    - Loading durumunda spinner veya skeleton gösteriliyor mu?
    - Error durumunda kullanıcıya anlamlı mesaj gösteriliyor mu?

DÜZELTME KURALLARI:
- Sadece HATA düzelt
- Eksik erişim kontrolü varsa EKLE
- Kırık sayfa varsa DÜZELT
- Eksik component varsa basit PLACEHOLDER oluştur
- Console hatası varsa ÇÖZE

FİNAL RAPOR:
Bu sefer şu formatta ver:

PLATFORM DURUMU:
🟢 Çalışan sayfalar: [liste]
🟡 Kısmi sorunlu: [liste + sorun açıklaması]  
🔴 Çalışmayan: [liste + neden]

GÜVENLİK DURUMU:
✅ Rol kontrolü olan endpoint sayısı: X
⚠️ Rol kontrolü eksik endpoint sayısı: X
🔒 Admin koruması: Var/Yok

GENEL SKOR: %X

ÖNERİLER:
- Acil düzeltilmesi gereken: [varsa]
- İyileştirme önerileri: [varsa]