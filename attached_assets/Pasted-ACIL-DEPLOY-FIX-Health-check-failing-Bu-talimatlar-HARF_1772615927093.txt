ACIL DEPLOY FIX — Health check failing. Bu talimatları HARFI HARFINE uygula.

SORUN: Replit deploy health check "/" endpoint'ine istek atıyor ama server cevap veremiyor çünkü AISStream, sanctions fetch ve geocoding server'ı bloke ediyor.

ADIM 1: Server'ın ana dosyasını bul ve aç. Bu dosya şunlardan biri:
- server/index.ts
- server/main.ts  
- server/app.ts
Hangisi "app.listen" içeriyorsa O dosya.

ADIM 2: O dosyada app.listen SATIRINI bul. app.listen'dan HEMEN ÖNCE şu kodu ekle:

app.get("/api/health", (_req, res) => {
  res.status(200).json({ status: "ok", timestamp: Date.now() });
});

ADIM 3: Aynı dosyada şu kelimeleri ARA ve HER BİRİNİ setTimeout içine al:

A) "aisstream" veya "AISStream" veya "ais" ile ilgili herhangi bir fonksiyon çağrısı — startAIS(), connectAIS(), initAIS(), setupAISStream() gibi:
BULDUĞUN ÇAĞRIYI SİL, yerine koy:
setTimeout(() => { try { BURAYA_ESKİ_ÇAĞRI(); } catch(e) { console.log("AIS skipped:", e.message); } }, 60000);

B) "sanctions" veya "OFAC" veya "SDN" ile ilgili fonksiyon çağrısı — fetchSanctions(), loadSanctions() gibi:
BULDUĞUN ÇAĞRIYI SİL, yerine koy:
setTimeout(() => { try { BURAYA_ESKİ_ÇAĞRI(); } catch(e) { console.log("Sanctions skipped:", e.message); } }, 90000);

C) "geocoding" veya "geocode" ile ilgili fonksiyon çağrısı:
BULDUĞUN ÇAĞRIYI SİL, yerine koy:
setTimeout(() => { try { BURAYA_ESKİ_ÇAĞRI(); } catch(e) { console.log("Geocoding skipped:", e.message); } }, 120000);

D) "cron" veya "schedule" veya "job" ile ilgili fonksiyon çağrısı:
BULDUĞUN ÇAĞRIYI SİL, yerine koy:
setTimeout(() => { try { BURAYA_ESKİ_ÇAĞRI(); } catch(e) { console.log("Cron skipped:", e.message); } }, 45000);

ADIM 4: app.listen callback'inde SADECE şu log olmalı:
app.listen(port, "0.0.0.0", () => {
  console.log(`Server is ready and listening on port ${port}`);
});

app.listen ile aynı callback içinde BAŞKA HİÇBİR FONKSİYON ÇAĞIRMA.

ADIM 5: Production'da frontend serve edilmeli. app.listen'dan ÖNCE, health check'ten SONRA:

import path from "path";
import fs from "fs";

const distPublicPath = path.resolve(process.cwd(), "dist/public");
const distPath = path.resolve(process.cwd(), "dist");
const staticPath = fs.existsSync(distPublicPath) ? distPublicPath : distPath;

if (fs.existsSync(staticPath)) {
  app.use(express.static(staticPath));
  app.get("*", (req, res, next) => {
    if (req.path.startsWith("/api")) return next();
    res.sendFile(path.join(staticPath, "index.html"));
  });
}

ADIM 6: Server başladıktan sonra console çıktısı TAM OLARAK şu sırada olmalı:
[HEMEN] "Server is ready and listening on port 5000"
[45sn sonra] cron job'lar
[60sn sonra] AISStream
[90sn sonra] sanctions
[120sn sonra] geocoding

BAŞKA HİÇBİR ŞEY app.listen'dan ÖNCE çalışmamalı. Hiçbir ağ isteği, hiçbir WebSocket bağlantısı, hiçbir dosya indirme app.listen ÖNCESİNDE olmamalı.

KONTROL: Değişiklikten sonra "npm run dev" yap. Console'da İLK SATIR "Server is ready" olmalı. AIS, sanctions, geocoding 45-120 saniye SONRA başlamalı.