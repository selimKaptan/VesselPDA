# VesselPDA - Maritime Platform

## Overview
VesselPDA is a professional web-based maritime platform designed to revolutionize interactions between ship agents, shipowners, and maritime service providers. Its primary purpose is to enable ship agents to generate instant proforma disbursement accounts efficiently and to connect shipowners with a comprehensive directory of maritime service providers. The platform aims to streamline maritime operations, enhance transparency, and foster a connected ecosystem within the shipping industry, ultimately becoming the leading digital hub for maritime professionals.

## User Preferences
I prefer detailed explanations and iterative development. Ask before making major changes. I would like to see the agent work through the problem step-by-step. I prefer clear and concise communication.

## System Architecture
The platform is built with a modern web stack, featuring a maritime-themed UI/UX with a deep blue color palette.

**Technical Implementations:**
- **Frontend**: React, Vite, Tailwind CSS, and Shadcn UI.
- **Backend**: Express.js with PostgreSQL and Drizzle ORM.
- **Authentication**: Custom email/password authentication with email verification and session management.
- **Core Features**: User & Role Management (6-role system: Shipowner, Agent, Broker, Provider, Master, Admin), Vessel & Port Management, Proforma Generation (formula-based, PDF export, live exchange rates), Company Profiles & Directory (Trust & Verification), Subscription System (3-tier), Vessel Tracking (Mapbox GL JS with live AIS), Port Call Tender System, Voyage Management, Service Request System, Forum/Discussion Board, Document Management, Mutual Assessment, Direct Messaging, Notification System, Internationalization (TR/EN), Dark Mode, Navigation System (Icon Rail + Expandable Panel), Admin User Management, Audit Log System, Vessel Certificate Management, Port Call Appointment Management, Fixture & Recap System (Laytime & Demurrage Calculator), Cargo & Position Board, Market Data, AI Assistant, Vessel Crew Management, Tariff Management System, DB-Connected Quick Estimate (redesigned Quick Cost Preview: two-column categorized LINE ITEMS + COST BREAKDOWN progress bars; `groupedBreakdown` in API responses; `ProformaLineItem.category`; `client/src/lib/proforma-summary.ts` with `generateProformaSummaryPdf()`/`generateProformaSummaryHtml()`; Export button), Organization & Team Management, Server-Side Cache Layer (4-tier in-memory), File System Storage (uploads to disk), Statement of Facts (SOF) Module (CRUD, PDF export), Final Disbursement Account (FDA) Module (CRUD, PDF export, variance calculation), DA Comparison Report Module (variance analysis, PDF export), Vessel Schedule Calendar (Month, Timeline, List views), Voyage Invitation System (participant management, role-based permissions), Onboarding Wizard (5-step for new users), Mobile Responsiveness (full support, bottom nav bar), Demo Account System (server-side seeding on registration via /register?demo=true), Q88 Vessel Questionnaire Module (10-tab form, auto-fill from vessel data, PDF export, email sharing, public access, vessel list badges), Glassmorphism Design System (all floating UI components), PDF Export System (Proforma/SOF/FDA — shared header/footer with company logo embed + company info + bank details; `server/proforma-pdf.ts` exports `addPdfHeader`/`addPdfFooter` for reuse), Company Profile Bank Details (6 fields: bankName, bankAccountName, bankIban, bankSwift, bankCurrency, bankBranchName stored in DB; PATCH /api/company-profile/bank-details endpoint; "Load from Company Profile" button in proforma-new; server auto-fills bank on POST /api/proformas). **SOF Laytime Factor** (`sofLineItems.laytimeFactor` integer column, default 100): replaces "Deductible" checkbox. 3-button toggle (100%/50%/0%) in "Add Event" modal and inline event cards. Auto-mapping by event type: rain_start/rain_stop/breakdown_start/breakdown_stop → 0%; shifting_start/shifting_end → 50%; all others → 100%. "✨ Auto-adjusted" indicator shown when factor was auto-set. Timeline dots: blue=100%, amber=50%, grey=0%.
- **Demo System**: Users access demo via `/register?demo=true`. Registration with `isDemo: true` in body triggers server-side seeding (5 vessels, 5 proformas, 5 voyages, 3 SOFs, 2 FDAs, 5 invoices, 3 fixtures, 4 cargo positions, 2 NORs, checklists, notifications). Users are immediately logged in (no email verification) and redirected to dashboard. `users` table has `demo_seeded` and `is_demo_account` boolean columns. Seed logic in `server/demo-seed.ts`, route in `server/routes/demo.routes.ts`.
- **Security**: Helmet Content-Security-Policy headers configured.
- **Berthing Fees Formula System**: `berthing_tariffs` table extended with `per_1000_gt` (real) and `gt_threshold` (integer, default 500) columns. `lookupBerthingFee` in `tariff-lookup.ts` uses formula mode when `per_1000_gt` is set: GT ≤ threshold → flat fee (`intl_foreign_flag`); GT > threshold → `Math.ceil(GT/1000) × per_1000_gt`. Turkish flag = `Math.ceil(foreign × 0.75)`, Cabotage = `Math.ceil(foreign × 0.50)`. Global seed row: `{intl_foreign_flag: 10, per_1000_gt: 25, gt_threshold: 500}`. Tariff Management UI shows formula summary card (3 service types with formulas + example at GT=2000). Quick-estimate default `berthStayDays` changed from 3 → 1.
- **Schema Integrity**: `shared/schema.ts` fully aligned with production DB — 24 tables corrected from `generatedAlwaysAsIdentity()` to `serial()` to match actual DB sequences; `organization_members.role_id` orphan column registered in schema (no FK reference); 18 production tariff tables added to schema (`pilotage_tariffs`, `external_pilotage_tariffs`, `agency_fees`, `marpol_tariffs`, `lcb_tariffs`, `tonnage_tariffs`, `cargo_handling_tariffs`, `berthing_tariffs`, `supervision_fees`, `custom_tariff_sections`, `custom_tariff_entries`, `misc_expenses`, `chamber_freight_share`, `harbour_master_dues`, `sanitary_dues`, `vts_fees`, `port_authority_fees`, `other_services`); `port_authority_fees` production columns fully matched (added `fee_name`, `fee_no`, `min`, `max`, `size_min`, `size_max`, `unit`, `multiplier_rule`); `drizzle.config.ts` has `tablesFilter` listing all 80 managed tables. Deployment schema drift issue fully resolved.
- **Navigation Architecture (IA Overhaul 2026-03)**: `client/src/lib/nav-categories.ts` — 10 categories total. **Port Operations** (Voyages, Proformas, NOR, SOF, Nominations, PDA Review). **Commercial** (Tenders, Fixtures, Cargo Positions, Laytime Calculator). **Fleet** (Vessels, Q88, Schedule, Certs, Tracking, Crew Roster). **Services & Directory** (Provider Directory, Service Requests, Ports DB). **Finance** (DA Comparison, FDA, Invoices). **Workspace** (Messages, Forum, Notifications, Voyage Invitations, AI Smart Drop). **Intelligence** (Market Data, Port Info, Sanctions Check). **Admin**. **Settings** (Settings, Team, Company Profile, Pricing & Plans). "Tools" category removed — all items redistributed. ROLE_CATEGORIES updated for all 6 roles. Cross-page links: Vessel card → New PDA (/proformas/new?vesselId=X); Voyage detail (Financial tab) → Create PDA (/proformas/new?voyageId=X); Voyage detail (Operation tab) → Create SOF (/sof?voyageId=X); Nomination accepted → Create PDA; Tender nomination banner → Create PDA. proforma-new.tsx reads `?vesselId=` URL param to auto-select vessel.
- **Crew Management Enhancements (2026-03)**: `vessel_crew` table extended with `medical_fitness_expiry`, `medical_fitness_file_base64/name/url`, `status` ("on_board"/"on_leave") columns. Crew tab in Vessels page: Medical Fitness Exp + Doc columns (upload/preview/download/remove), dynamic tooltip on ⚠/✕ icons ("Expires in X days" / "Expired X days ago") via Shadcn Tooltip + TooltipProvider wrap, "Export All Docs" CSV button, Status select + Medical Fitness Expiry in dialog. New `/crew-roster` page (Fleet nav): all crew across fleet with vessel name, status badge (green/amber), filter buttons (All/On Board/On Leave), text search, tooltip expiry cells. Backend: GET /api/vessels/crew-roster (storage.getCrewRoster with JOIN on vessels). PATCH handler extracts medicalFitnessFileBase64 like passport/seamansBook.
- **Cargo Ops Tab — Command Center (Voyage Detail)**: `voyageCargoLogs` table (id, voyageId, logDate, shift, fromTime, toTime, receiverId, amountHandled, truckCount, batchId, cumulativeTotal, logType ["operation"|"delay"], remarks, createdBy) + `cargoTotalMt: real` on voyages + `voyageCargoReceivers` (id, voyageId, name, allocatedMt). API: GET/POST/DELETE `/api/voyages/:id/cargo-logs`, DELETE `/api/voyages/:id/cargo-logs/batch/:batchId`, PATCH `/api/voyages/:id/cargo-total`, GET/POST/DELETE `/api/voyages/:id/cargo-receivers`. **Direct Delivery multi-receiver support**: POST accepts `entries: [{receiverId, amountHandled, truckCount}]` array; server generates UUID `batchId` and inserts one row per receiver. Frontend tab "Cargo Ops" in voyage-detail.tsx: **Active Receiver Banner** (pulse green dot + "CURRENTLY DISCHARGING: IFFCO & Kucukbay, Since HH:MM" — all receivers from last batch joined with " & "); main progress bar; **Cargo Parcels** section with per-receiver mini bars + per-receiver ETC "Est: DD Mon HH:MM"; **3 metric cards** (avg rate, ETC with date+time+countdown "in ~Xh Ym", operation type badge); **Operation Logs table** (batchId-grouped rows, multi-receiver color badges per row, "Handled (MT & Trucks)" with truck count in grey sub-line, Period Rate MT/H with TrendingUp↑/TrendingDown↓, delay rows bg-red-900/20, "Waiting for Trucks" rows bg-amber-900/20 + amber ⚠ icon). Add Log modal: **Operation mode** = per-receiver grid (Receiver | Amount MT | Trucks # per row, truck estimate hint "@24 MT/truck"); **Delay mode** = Delay Reason dropdown ("⚠ Waiting for Trucks/Tankers", Rain, Breakdown, Shift Change, Customs, Hatch Shift, Other) + optional notes textarea. IIFE pattern for scoped computed vars + batchMap/groupedLogs + lastBatch/activeReceiverNames + BADGE_COLORS.
- **Voyage Contacts Module**: `voyage_contacts` table (id, voyageId FK cascade, email, name, role ['owner'|'charterer'|'receiver'|'sub_agent'|'other'], includeInDailyReports bool, createdAt). New "Contacts" tab in voyage-detail (8th tab, Mail icon). **Bulk Paste**: textarea → POST /api/voyages/:id/contacts/bulk parses comma/semicolon/newline separated emails, skips duplicates. **Contact List**: per-contact role Select (PATCH on change) + Daily Reports Switch toggle + delete button. **Single add form**: email + name + role. **Send Cargo Report Integration**: dialog auto-loads contacts with role owner/charterer/receiver + includeInDailyReports=true → pre-checked checkboxes; user can uncheck; manual extra email input; blue summary "Will send to N addresses"; `POST /api/voyages/:id/send-cargo-report` updated to accept `toEmails: string[]` (Resend `to: array`); `sendCargoReportEmail()` updated accordingly.
- **AI Smart Drop Module**: Drag-and-drop / paste document analysis using Claude claude-haiku-4-5. Detects 11 maritime event types (crew_change, eta_update, nor_tendered, etc.). Glassmorphism drop zone component (`client/src/components/ai-smart-drop.tsx`). Analysis result modal with edit mode and confirm & process routing. 5/min rate limiter. DB table `ai_analysis_history`. Endpoints: POST /api/ai/parse-document, POST /api/ai/execute-action, GET /api/ai/history. Dashboard widget (AiSmartDropMini) on all 5 role dashboards. Full page at /ai-smart-drop under **Workspace** navigation (moved from Tools).

## External Dependencies
- **PostgreSQL**: Primary database.
- **bcryptjs**: For password hashing.
- **AISStream.io**: For live AIS vessel tracking data.
- **RapidAPI (Zyla Labs Vessel Information API)**: For detailed vessel information.
- **Turkish Central Bank (TCMB)**: For live exchange rates.
- **Resend**: Transactional email service.
- **jsPDF + html2canvas**: For client-side PDF generation.
- **Mapbox GL JS**: For interactive maps.
- **Vite**: Frontend build tool.
- **Tailwind CSS**: CSS framework.
- **Shadcn UI**: UI component library.
- **Recharts**: Charting library.
- **Anthropic AI**: For the AI Assistant (Claude-Haiku).
- **Yahoo Finance API**: For market data indices (BDI, BCTI, BDTI).